#include <stdio.h>

void Tabuada(int N) {
    printf("Tabuada do %d:\n", N);
    for (int i = 1; i <= 9; i++) {
        printf("%d x %d = %d\n", N, i, N * i);
    }
    printf("\n");
}

int main() {
    for (int i = 1; i <= 9; i++) {
        Tabuada(i);
    }

    return 0;
}
